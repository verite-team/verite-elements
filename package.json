{
  "private": true,
  "scripts": {
    "build": "turbo build",
    "build:stencil": "turbo run build --force --filter=stencil-react --filter=stencil-angular",
    "dev": "turbo dev",
    "storybook": "turbo storybook",
    "lint": "turbo lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "clean": "turbo clean",
    "react:dev": "turbo dev --filter=react-client",
    "angular:dev": "turbo dev --filter=angular-client"
  },
  "devDependencies": {
    "eslint": "^9.14.0",
    "prettier": "^3.3.3",
    "turbo": "^2.3.0"
  },
  "packageManager": "pnpm@9.13.1",
  "name": "with-vite"
}